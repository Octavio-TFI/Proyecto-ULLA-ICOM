{
  "schema": 1,
  "description": "Evalua la relevancia de un documento recuperado",
  "execution_settings": {
    "default": {
      "max_tokens": 1000,
      "temperature": 0.2,
      "top_p": 0.1,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0,
      "response_format": {
        "type": "object",
        "properties": {
          "Score": {
            "type": "boolean"
          }
        },
        "required": [
          "Score"
        ]
      }
    }
  },
  "input_variables": [
    {
      "name": "document",
      "description": "Documento recuperado"
    },
    {
      "name": "consulta",
      "description": "Consulta del usuario"
    }
  ]
}